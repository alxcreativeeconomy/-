/**
 * Copy to stripe-config.js after creating a Stripe account.
 * Publishable key: Stripe Dashboard → Developers → API keys → Publishable key
 */
window.STRIPE_CONFIG = {
  publishableKey: "pk_test_YOUR_PUBLISHABLE_KEY",
};